services:
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./certs:/app/certs  # Important for SSL connection!
    env_file:
      - .env
    restart: unless-stopped